# Vimal Pets World Android app

This version removes the custom TWA splash/FileProvider configuration that could cause the launcher to close immediately, and adds the official WebView fallback.

Website: https://vimal27.in/
Build: `gradle assembleDebug`

After building, the APK is:
`app/build/outputs/apk/debug/app-debug.apk`
